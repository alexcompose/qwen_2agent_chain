# qwen_2agent_chain
Deterministic 2-agent LangGraph pipeline using a local Qwen model. Agent 1 generates a technical draft via chat templates, and Agent 2 acts as a strict editor to clean up structure and remove meta-talk in a sequential graph flow.
